<!--
  Global APIM Policy — Azure API Platform
  Applied to all APIs in the APIM instance.

  Enforces:
  1. Read-only access (GET/HEAD only)
  2. JWT validation via Entra ID
  3. Rate limiting per subscription
  4. Security header stripping
-->
<policies>
  <inbound>
    <!-- ============================================================= -->
    <!-- Read-Only Enforcement: reject all methods except GET and HEAD  -->
    <!-- ============================================================= -->
    <choose>
      <when condition="@(context.Request.Method != &quot;GET&quot; &amp;&amp; context.Request.Method != &quot;HEAD&quot;)">
        <return-response>
          <set-status code="405" reason="Method Not Allowed" />
          <set-header name="Content-Type" exists-action="override">
            <value>application/json</value>
          </set-header>
          <set-body>{"error": "Only GET and HEAD methods are permitted on this API."}</set-body>
        </return-response>
      </when>
    </choose>

    <!-- ============================================================= -->
    <!-- JWT Validation (Entra ID / Azure AD)                          -->
    <!-- Replace {{tenant-id}} and {{api-audience}} with named values   -->
    <!-- ============================================================= -->
    <validate-jwt header-name="Authorization" failed-validation-httpcode="401" require-scheme="Bearer">
      <openid-config url="https://login.microsoftonline.com/{{tenant-id}}/v2.0/.well-known/openid-configuration" />
      <audiences>
        <audience>{{api-audience}}</audience>
      </audiences>
      <issuers>
        <issuer>https://sts.windows.net/{{tenant-id}}/</issuer>
        <issuer>https://login.microsoftonline.com/{{tenant-id}}/v2.0</issuer>
      </issuers>
      <required-claims>
        <claim name="roles" match="any">
          <value>Data.Read</value>
        </claim>
      </required-claims>
    </validate-jwt>

    <!-- ============================================================= -->
    <!-- Rate Limiting: 100 calls per 60 seconds per subscription      -->
    <!-- ============================================================= -->
    <rate-limit-by-key calls="100" renewal-period="60"
      counter-key="@(context.Subscription?.Key ?? context.Request.IpAddress)" />

    <!-- ============================================================= -->
    <!-- Forward Function App host key for backend authentication      -->
    <!-- ============================================================= -->
    <set-header name="x-functions-key" exists-action="override">
      <value>{{function-app-key}}</value>
    </set-header>

    <!-- Add correlation ID for request tracing -->
    <set-header name="X-Correlation-Id" exists-action="skip">
      <value>@(context.RequestId.ToString())</value>
    </set-header>

    <base />
  </inbound>

  <backend>
    <base />
  </backend>

  <outbound>
    <!-- Strip internal/sensitive headers -->
    <set-header name="X-Powered-By" exists-action="delete" />
    <set-header name="X-AspNet-Version" exists-action="delete" />
    <set-header name="Server" exists-action="delete" />

    <!-- Add security headers -->
    <set-header name="X-Content-Type-Options" exists-action="override">
      <value>nosniff</value>
    </set-header>
    <set-header name="X-Frame-Options" exists-action="override">
      <value>DENY</value>
    </set-header>
    <set-header name="Strict-Transport-Security" exists-action="override">
      <value>max-age=31536000; includeSubDomains</value>
    </set-header>

    <base />
  </outbound>

  <on-error>
    <base />
    <set-header name="Content-Type" exists-action="override">
      <value>application/json</value>
    </set-header>
    <set-body>@{
      return new JObject(
        new JProperty("error", context.LastError.Message),
        new JProperty("request_id", context.RequestId.ToString())
      ).ToString();
    }</set-body>
  </on-error>
</policies>
